<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/kinetic_surface">

    <!-- Header Moderno -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerGemini"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:paddingHorizontal="24dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvTituloIA"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:text="SPORTSGO AI"
            android:textAllCaps="true"
            android:textColor="@color/kinetic_primary"
            android:textSize="20sp"
            android:textStyle="italic"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="ASISTENTE VIRTUAL"
            android:textColor="@color/kinetic_text_gray"
            android:textSize="10sp"
            app:layout_constraintStart_toStartOf="@id/tvTituloIA"
            app:layout_constraintTop_toBottomOf="@id/tvTituloIA" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Lista de Mensajes -->
    <ListView
        android:id="@+id/lvChatGemini"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="16dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        android:stackFromBottom="true"
        android:transcriptMode="alwaysScroll"
        app:layout_constraintBottom_toTopOf="@id/inputArea"
        app:layout_constraintTop_toBottomOf="@id/headerGemini" />

    <!-- Area de Entrada Moderna -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/inputArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardBackgroundColor="@color/kinetic_card"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:strokeColor="@color/kinetic_primary_glow"
        app:strokeWidth="1dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp">

            <EditText
                android:id="@+id/etMensajeGemini"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@null"
                android:fontFamily="sans-serif-medium"
                android:hint="Pregunta sobre tu entreno..."
                android:textColor="@color/white"
                android:textColorHint="@color/kinetic_text_gray"
                android:textSize="14sp"
                android:maxLines="4"
                android:padding="12dp" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btnEnviarGemini"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:backgroundTint="@color/kinetic_primary"
                app:fabSize="mini"
                app:srcCompat="@android:drawable/ic_menu_send"
                app:tint="@color/kinetic_surface"
                app:elevation="0dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>